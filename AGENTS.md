# OpenFit agent instructions

Before any deployment, VPS, Docker, OAuth, synchronization, or production-diagnostics task, read `docs/OPERATIONS.md` completely. Also read `.agents/OPERATIONS.local.md` when it exists; it contains machine-local access details and is intentionally ignored by Git.

Preserve unrelated user work. In particular, do not stage, modify, or delete `docs/PRD_AMAZFIT_INTEGRATION.md` unless the user explicitly requests it.

When operating as Claude Fable, use the `/efficient-fable` skill always.
